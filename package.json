{
	"name": "radahn",
	"version": "0.1.0",
	"description": "A programming language designed for coding agents",
	"type": "module",
	"bin": {
		"radahn": "./src/cli.ts"
	},
	"scripts": {
		"build": "bun build src/cli.ts --compile --outfile radahn",
		"test": "bun test",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"format:check": "biome format ."
	},
	"dependencies": {
		"commander": "^13.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.0",
		"@types/bun": "latest"
	}
}
